[package]
name = "lensos-files"
version = "0.1.0"
edition = "2021"
authors = ["LensOS Team"]
description = "LensOS v0.1 Files Application Module"

[dependencies]
# Standard library is used for maximum performance, security, and portability in LensOS.
