[package]
name = "lensos-installer"
version = "0.1.0"
edition = "2021"
authors = ["LensOS Architecture Team <engineering@lensos.org>"]
description = "Installer, package manager, and system recovery engine for LensOS v0.1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
