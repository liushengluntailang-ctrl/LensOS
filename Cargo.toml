[package]
name = "lens-browser"
version = "0.1.0"
edition = "2021"
authors = ["LensOS Architecture Team <dev@lensos.org>"]
description = "LensOS v0.1 Browser Application Core Engine"
license = "MIT OR Apache-2.0"

[dependencies]
# LensOS Browser is implemented in clean, pure standard Rust to ensure
# ultra-fast compilation, zero memory overhead, and portable execution across LensOS runtimes.
