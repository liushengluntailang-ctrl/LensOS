[package]
name = "lensos-runtime"
version = "0.2.0"
edition = "2021"
authors = ["LensOS Team <core@lensos.dev>"]
description = "LensOS v0.2 Operating System Runtime Engine, Compositor, Framebuffer & Input Subsystem"
license = "MIT OR Apache-2.0"
readme = "README.md"
keywords = ["os", "kernel", "runtime", "framebuffer", "compositor"]
categories = ["no-std", "os::kernel-dev"]

[features]
default = ["std"]
std = []
qemu-debug = []

[dependencies]
