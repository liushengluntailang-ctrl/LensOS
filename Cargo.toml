[package]
name = "lens_ai"
version = "0.1.0"
edition = "2021"
authors = ["LensOS Team"]
description = "LensAI application module for LensOS operating system"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1.0"
