[package]
name = "lensos-settings"
version = "0.1.0"
edition = "2021"
authors = ["LensOS Team <dev@lensos.org>"]
description = "Modular Rust settings application for LensOS with frosted glass aesthetics, kernel IPC, and AI integration."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
